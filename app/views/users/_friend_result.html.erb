<div class="results-block">
  <%= render 'layouts/messages' %>
</div>

<% if @friend %>
<div class="card card-header results-block">
  <strong>Email: </strong> <%= @friend %>
  <strong>First Name: </strong> <%= @friend %>
  <strong>Last Name: </strong> <%= @friend %>
  <%= link_to 'Follow friend?', '#', method: :post, class: "btn btn-success" %>
  <%# if current_user.can_track_stock?(@stock.ticker) %>
    <%#= link_to "Add to portfolio", user_stocks_path(user: current_user, ticker: @stock.ticker), method: :post, class: "btn btn-success" %>
  <%# else %>
    <!--
    <span class="badge badge-secondary">
      You are already tracking
      <%# if !current_user.under_stock_limit? %>
        10 stocks
      <%# end %>
      <%# if current_user.stock_already_tracked?(@stock.ticker) %>
        this stock
      <%# end %>
    </span>
    -->
  <%# end %>
</div>
<% end %>
